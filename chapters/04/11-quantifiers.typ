#import "../../macros.typ": *

== Quantifiers

#todo[
  - Only ever triggered `forall` inside domain axioms in this fragment — there
    are no quantified permissions anywhere in the corpus.
  - Trigger heads, and how a trigger is matched against the e-graph
    (e-matching) rather than handed to Z3.
  - Instantiation control: what stops saturation from running away.

  #para[Run-in headings] Triggers / E-matching / Instantiation / What we record
]
